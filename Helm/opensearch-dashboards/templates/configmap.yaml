{{- if .Values.dashboardsConfig }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "opensearch-dashboards.fullname" . }}-config
  labels: {{ include "opensearch-dashboards.labels" . | nindent 4 }}
data:
  {{- range $path, $config := .Values.dashboardsConfig }}
  {{ $path }}: |
  {{ tpl $config $ | indent 4 -}}
  {{- end -}}
  {{- end -}}
